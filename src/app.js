import { add, subtraction } from './math.js';

const result = math.add(5, 3);
console.log(`The result of adding 5 and 3 is: ${result}`);

const difference = math.subtraction(10, 4);
console.log(`The result of subtracting 4 from 10 is: ${difference}`);